[package]
name = "siege-math"
version = "0.2.0"
authors = ["Mike Dilger <mike@optcomp.nz>"]

[dependencies]
# FIXME: once simd is part of rust std and stable, use it.
# simd = "0.2"
num-traits = "0.1"
float-cmp = "0.2"
serde = "1.0"
serde_derive = "1.0"
